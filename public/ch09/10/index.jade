extends ../layout

block vars
  - var body_attrs = {'ng-controller':'ToastController'}
  - var script = '10.js'
  - var title = "Toast with template"

block content
  md-toolbar.md-toolbar-tools
    h1 Toast with template
  md-content(layout-align='start center')
    md-button.md-raised.md-primary(
      ng-click='showToast()'
      ng-disabled='toastVisible'
    ) Show
    md-button.md-raised.md-accent(
      ng-click='dismissToast()'
      ng-disabled='!toastVisible'
    ) Dismiss
    div(flex,layout-padding='')
      label {{dismissAction}}

append scripts
  script
    :coffee-script
      angular.module 'myApp', ['ngMaterial', 'toast']
