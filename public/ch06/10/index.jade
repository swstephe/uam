extends ../layout

block vars
  - var title = "Checkboxes"

block content
  // Single column
  md-toolbar.md-toolbar-tools
    h1 Dive gear checklist
  md-content
    div(layout='column',layout-margin,ng-init='gear={}')
      md-checkbox(ng-model='gear.fins') Fins
      md-checkbox.md-primary(ng-model='gear.suit') Neoprene suit
      md-checkbox(ng-model='gear.bcd') BCD jacket
      md-checkbox(ng-model='gear.regulator') Regulator
      md-checkbox(ng-model='gear.tank') Tank
      md-checkbox(ng-model='gear.weights') Weights
    md-divider
    div(layout-margin)
      code {{gear|json}}

append scripts
  script
    :coffee-script
      angular.module 'myApp', ['ngMaterial']
