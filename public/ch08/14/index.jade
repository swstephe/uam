extends ../layout

block vars
  - var controller = 'GridListController'
  - var script = '14.js'
  - var style = '14.css'
  - var title = "Grid list"

block content
  md-toolbar.md-toolbar-tools
    h1 Grid list
  md-content
    md-grid-list(md-row-height='1:1',md-cols=8,md-cols-sm=3)
      md-grid-tile(
        ng-repeat='item in palette'
        ng-class="item.light ? 'light-text' : 'dark-text'"
        ng-style="{'background-color': item.color}"
        md-rowspan='{{calcRowspan($index)}}'
        md-colspan='{{calcColspan($index)}}'
        md-rowspan-sm=1
        md-colspan-sm=1
      ) {{item.name}}

append scripts
  script
    :coffee-script
      angular.module 'myApp', ['ngMaterial', 'gridlist']
